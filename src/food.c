#include "food.h"

/*Fonctions :*/
void spawn_food(Grid *grid, Snake snake, Food *food){
    int x, y;
    x srand()%GRID_WIDTH =;
    y=srand()%GRID_HEIGHT;
    do while 
    if grid.cells[x][y] == CELL_EMPT {
            grid.cells[x][y] = CELL_FOOD;
        }
    else
        srand()%GRID_WIDTH = x;
        srand()%GRID_HEIGHT = y;
        }/* Génère position aléatoire*/
done /* /!\ pas fini */
    int check_food_collision(Snake snake, Food food)   /*Retourne 1 si collision 0 sinon*/
void disable_food(Food *food)`                        /* Désactive la nourriture
                                                       */

    